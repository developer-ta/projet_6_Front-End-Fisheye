//import { _ProfilePartiale } from './_photographerProfile.js';

import { _mainSection } from "./_photographerMain.js";

export const pageBuilder = (MediasData, phData) => {
  _mainSection(MediasData, phData)
};

/**
//Mettre le code JavaScript lié à la page photographer.html
const initPage = (mediasData, photographer) => {};
const updatePage = () => {};
const render = () => {};


 // partiale views


//header part
const _headerPartiale = () => {};

//main part
const _mainSection = () => {};
const _sortBySelectPartiale = () => {};
// main > gallery
const _gallerySection = () => {};
const _likePartiale = () => {};
const _likeTotalPartiale = () => {};

// lightbox part
const _lightboxModal = () => {};
//form modal
const _formModal = () => {}; */

// 848 / 5 000
// Résultats de traduction
// Résultat de traduction
// Displays a gallery of the photographer's work.
// ○ Photographers can show both photos and videos.
// ■ For videos, show a thumbnail image in the
// gallery.
// ○ Each media includes a title and a number of likes.
// ■ When the user clicks on the "Like" icon, the number of likes
// displayed is incremented.
// ■ A photographer’s total number of likes must match the
// sum of likes from each of its media.aw
// ○ Media can be sorted by popularity or title.
// ○ When the user clicks on a media, it should open in a
// lightbox:
// ■ When the lightbox is displayed, there is a cross in the corner to
// close the window.
// ■ Navigation buttons allow you to move between items
// media to another in the lightbox (users can click
// on these buttons to navigate)
